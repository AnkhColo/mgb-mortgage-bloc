//Authentication:
//Essentially --> setup the functionalities fron the Firebase SDK, specifically sign
import {
  createUserWithEmailAndPassword,
  signInWithEmailAndPassword,
  signOut,
  onAuthStateChanged,
} from "firebase/auth";
